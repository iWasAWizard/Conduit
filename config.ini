[SYSLOG]
enable=yes
remote=no
# Enable if remote is "yes", otherwise use system syslog
#syslog_dest=<dest_ip>
#syslog_port=<dest_port>
log_level=info

[APP]
# pick what modules you need out of the sub-repos (TODO: make sub-repos)
modules=["cli", "fileops", "ssh", "vmware"]
# enable this to bomb the test immediately after a failure, instead of trying to press on
fail_fast=yes

[VMWARE]
#esxi_server="<esxi_ip>"
#esxi_user="<esxi_user>"
#esxi_pass="<esxi_pass>"